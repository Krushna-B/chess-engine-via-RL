[project]
name = "chess-training"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Krushna-B", email = "Krushna.Bhanushali@unc.edu" }
]
requires-python = ">=3.12"
dependencies = [
    "modal>=1.5.5",
    "numpy>=2.5.2",
    "torch>=2.14.0",
]

[project.scripts]
chess-training = "chess_training:main"

[build-system]
requires = ["uv_build>=0.10.10,<0.11.0"]
build-backend = "uv_build"
